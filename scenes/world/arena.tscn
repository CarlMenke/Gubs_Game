[gd_scene load_steps=2 format=3 uid="uid://bgubarena0001"]

; Whisperbloom Hollow. Deliberately a one-node scene: everything in the map is
; generated at load from `Net.config.map_seed` (D-007), so there is nothing here
; to hand-edit and nothing that can drift out of sync between clients. See
; `scripts/world/arena.gd` for the build order.

[ext_resource type="Script" path="res://scripts/world/arena.gd" id="1_arena"]

[node name="Arena" type="Node3D"]
script = ExtResource("1_arena")
